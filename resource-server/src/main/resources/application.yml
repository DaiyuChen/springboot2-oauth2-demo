server:
  port: 8000

spring:
  application:
    name: Resource Server

security:
  oauth2:
    client:
      client-id: test
      client-secret: 123456
      scope: TEST,USER,ORDER
    resource:
      check-token-uri: http://localhost:9000/oauth/check_token
      token-info-uri:  http://localhost:9000/oauth/check_token


